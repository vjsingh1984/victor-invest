"""
Pytest configuration for metric_extraction tests.

Provides fixtures for testing the metric extraction framework.
Uses standard imports since the package structure is now properly set up.
"""

import pytest

# The metric_extraction package is now properly structured with standard imports.
# No special module loading is needed - just use regular imports in test files.
